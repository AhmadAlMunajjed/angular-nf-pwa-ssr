import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gettajer.shell',
  appName: 'Shell App',
  webDir: 'dist/shell/browser'
};

export default config;
